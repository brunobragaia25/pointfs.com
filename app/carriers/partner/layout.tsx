import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Become a Carrier Partner",
  description:
    "Apply to become a Point Freight Systems carrier partner and start hauling quality freight today.",
};

export default function CarrierPartnerLayout({ children }: { children: React.ReactNode }) {
  return children;
}
